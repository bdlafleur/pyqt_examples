In order to create custom widgets, I needed to promote the widgets.

To do so, I promoted the widget and set the 'Promoted class name' to the name
of the class, and set the 'Header file' to be dropwidgets.py because it
contains both classes.

